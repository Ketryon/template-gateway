# App
NEXT_PUBLIC_APP_URL=http://localhost:3009

# External API — your existing backend provider(s)
API_BASE_URL=https://your-backend.example.com
API_KEY=

# CORS — comma-separated frontend origins allowed to call /api routes
ALLOWED_ORIGINS=http://localhost:3000
